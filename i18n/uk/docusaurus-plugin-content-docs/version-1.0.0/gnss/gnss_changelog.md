---
id: gnss_changelog
title: Історія версій та дорожня карта
sidebar_label: Changelog / Roadmap
---

На цій сторінці описано **історію змін прошивки** для плати **GNSS sanity board на STM32F401**, а також **орієнтовну дорожню карту** розвитку проєкту.

> 🔁 Плейсхолдер для зображення “версії / релізи”:
>
> `![Історія версій GNSS sanity board](pathname://IMAGE_URL_CHANGELOG_HEADER)`

---

## 1. Як читати цю сторінку

- Розділ **“Історія версій”** — короткий перелік релізів з основними змінами.
- Розділ **“Деталі релізів”** — більш розгорнутий опис кожної версії.
- Розділ **“Дорожня карта”** — заплановані напрямки розвитку, які *можуть змінюватися*.
- Розділ **“Як перевірити версію прошивки”** — де побачити номер версії у логах/коді.

Цей документ призначений як для інженерів, так і для операторів, щоб було легко зрозуміти, **яка версія прошивки стоїть на платі** і чим вона відрізняється від попередніх.

---

## 2. Історія версій (огляд)

> ⚠️ Номери версій нижче наведені як приклад структури. Будь ласка, відкоригуйте їх відповідно до реальних релізів вашої прошивки.

| Версія | Дата        | Короткий опис                                  |
|--------|-------------|-------------------------------------------------|
| v0.1.0 | 2025-MM-DD  | Перший робочий прототип, базові тригери DR     |
| v0.2.0 | 2025-MM-DD  | Додано STATUSTEXT, покращені пороги спуфінгу   |
| v0.3.0 | 2025-MM-DD  | Підтримка F10N, рефакторинг логів, DR-патерни  |
| v0.4.0 | 2025-MM-DD  | Підтримка MAVLink GPS_INPUT, дрібні виправлення|

> Ви можете змінити/доповнити таблицю під свої реальні релізи:
>
> - оновити дати;
> - скоригувати описи;
> - додати додаткові рядки (v0.4.1, v0.5.0 тощо).

---

## 3. Деталі релізів

### 3.1. Версія v0.4.0

**Статус:** поточний рекомендований реліз (приклад, змініть за потреби).

**Основні зміни:**

- впроваджено відправку GPS через `GPS_INPUT` (MAVLink) або актуальний вибраний тип;
- уніфіковано формати логів (`fix=.. s=.. hd=.. DR0/DR1`);
- оновлено логіку LED:
  - повільне миготіння для `DR0`;
  - швидке миготіння / “alarm” для `DR1`;
- дрібні виправлення у тригерах стрибка відстані/швидкості.

**Рекомендації до застосування:**

- використовується як основна версія для польотів;
- рекомендовано для всіх нових інсталяцій.

---

### 3.2. Версія v0.3.0

**Основні зміни:**

- додано повну підтримку GNSS F10N (парсинг NAV-PVT / відповідних NMEA);
- логіка DR адаптована під багаточастотні приймачі;
- поліпшено читабельність USB-логів (додано ключові поля і позначення).

**Відомі обмеження:**

- частина порогів налаштована консервативно, можливі зайві переходи в DR у складних умовах;
- не всі крайові сценарії для інших GNSS-модулів протестовані.

---

### 3.3. Версія v0.2.0

**Основні зміни:**

- додано відправку `STATUSTEXT` в автопілот:
  - `GPS: ENTER DR (jump)`;
  - `GPS: ENTER DR (no fix)`;
  - `GPS: EXIT DR`;
  - `SPOOF: SOUTH HEMI JUMP` тощо;
- поліпшено обробку помилок GNSS:
  - часові таймаути для відсутності fix;
  - відслідковування якості GNSS через HDOP, кількість супутників.

**Значення релізу:**

- полегшує аналіз у Mission Planner, дозволяє операторам швидко зрозуміти, чому ввімкнувся DR.

---

### 3.4. Версія v0.1.0

**Перший робочий прототип:**

- читання GNSS по UART;
- базова перевірка стрибка відстані та швидкості;
- простий DR-режим з логами лише по USB;
- передавання GPS-даних у автопілот через базовий MAVLink-формат.

**Відомі обмеження:**

- відсутні `STATUSTEXT` та розширені логи;
- недостатньо гнучка конфігурація порогів (частина значень захардкожена);
- немає чіткої індикації LED для різних станів DR.

---

## 4. Дорожня карта (Roadmap)

> ⚠️ Це орієнтовний список планів. Пріоритети та строки можуть змінюватися залежно від реальних потреб експлуатації.

### 4.1. Ближчі плани (короткий горизонт)

- **Гнучкі налаштування без перескладання коду:**
  - винесення ключових порогів (`GNSS_MAX_DIST_JUMP_M`, `GNSS_MAX_SPEED_MPS` тощо) у конфігураційний блок з можливістю змін через MAVLink або EEPROM.
- **Покращення логів:**
  - додаткові поля в USB-логах (наприклад, тип активного тригера DR);
  - систематичний формат для парсингу логів скриптами.
- **Режим “dry-run”:**
  - коли плата тільки аналізує GNSS і генерує попередження, але **не втручається** в GPS, який йде до автопілота (корисно для початкових тестів).

---

### 4.2. Середньострокові плани

- **Підтримка додаткових GNSS-приймачів:**
  - UM980, F9P у різних конфігураціях, інші UART-GNSS;
  - автоматичне розпізнавання формату (UBX/NMEA).
- **Більш складні детектори спуфінгу:**
  - аналіз зміни висоти та швидкості одночасно;
  - контроль часу (`time_of_week`, “стрибки” GPS-часу);
  - крос-перевірка з барометром / IMU (через додаткові канали, якщо буде реалізовано).
- **Проста телеметрія стану DR у GCS:**
  - окремі MAVLink-повідомлення про стан фільтру;
  - можливість виводити DR-статус у HUD/OSD.

---

### 4.3. Довгострокові ідеї

- **Підтримка CAN / UAVCAN:**
  - плата як CAN-модуль з виходом “фільтрованого GPS”.
- **Інтеграція з INS/IMU високої точності:**
  - більш складний фільтр на основі INS + GNSS;
  - розширене Dead Reckoning з використанням IMU навіть при повній втраті GNSS.
- **Автоматичні self-test при старті:**
  - перевірка памʼяті, UART-ліній, базових функцій до подачі GPS в автопілот.

---

## 5. Як перевірити версію прошивки

Конкретний спосіб залежить від того, як саме версія реалізована у вашому коді. Нижче наведені рекомендовані варіанти.

### 5.1. Вивід у USB-логах при старті

Рекомендовано виводити рядок типу:

~~~text
GNSS sanity board FW v0.4.0 (build 2025-01-23)
~~~

Тоді при підключенні по USB ви одразу бачите актуальну версію.

---

### 5.2. Вивід через STATUSTEXT при старті

Можна додати повідомлення:

~~~text
FW: GNSS sanity v0.4.0
~~~

яке один раз надсилається в автопілот після ініціалізації. Його буде видно у вкладці **Messages** Mission Planner.

---

### 5.3. Поле/константа в коді

Рекомендовано мати у коді щось на кшталт:

~~~cpp
static const char* FW_VERSION = "v0.4.0";
~~~

і використовувати цю константу:

- при формуванні стартового логу;
- при побудові `STATUSTEXT` на старті;
- у разі необхідності — для внутрішніх перевірок сумісності.

---

## 6. Політика оновлення прошивки

Рекомендовано дотримуватися таких правил:

1. **Перед оновленням:**
   - зберігати поточний `.bin` та зазначати, на якій саме платформі/UAV він стоїть;
   - робити резервні копії параметрів автопілота (`.param` файл);
   - виконувати короткі lab-тести нової версії.

2. **Після оновлення:**
   - відразу перевіряти USB-логи та `STATUSTEXT` на предмет версії;
   - проводити тестові польоти в безпечних умовах;
   - документувати, які проблеми вирішено, а які ще залишилися.

3. **Версіонування:**
   - збільшувати **мінорну версію** (v0.3 → v0.4), коли додаються нові функції;
   - збільшувати **патч-версію** (v0.3.0 → v0.3.1), коли виправляються лише баги без зміни логіки DR.

---

Підтримка акуратної історії версій та прозорої дорожньої карти значно спрощує:

- обслуговування флоту UAV;
- аналіз інцидентів;
- планування подальших оновлень і розширень функціональності GNSS sanity board.
