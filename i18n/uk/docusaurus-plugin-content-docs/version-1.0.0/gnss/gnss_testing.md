---
id: gnss_testing
title: Тестування та валідація плати GNSS sanity board
sidebar_label: Тестування
---

Цей розділ описує, **як перевірити роботу плати GNSS sanity board на STM32F401**:

- на столі (lab-тести),
- разом з автопілотом,
- у реальному польоті,
- із симульованим сигнальним середовищем (HackRF / gps-sdr-sim / PortaPack H2).

> 🔁 Плейсхолдер для схеми “lab → bench → flight”:  
> `![Етапи тестування GNSS sanity board](pathname://IMAGE_URL_TESTING_FLOW)`

---

## 1. Цілі тестування

Перш ніж використовувати плату на бойових/критичних місіях, необхідно підтвердити:

1. **Базову працездатність:**
   - плата стартує, LED працює;
   - є USB-логи з адекватними значеннями GNSS.
2. **Коректну інтеграцію з автопілотом:**
   - ArduPilot бачить GPS (через MAVLink);
   - немає дивних переходів у DR у нормальних умовах.
3. **Очікувану реакцію на проблеми GNSS:**
   - втрата fix;
   - поганий HDOP / мало супутників;
   - грубі стрибки координат / швидкості (імітація спуфінгу).
4. **Стійкість до керованого спуфінгу** у контрольованому середовищі (HackRF / PortaPack).

---

## 2. Lab-тест №1 — базовий запуск з GNSS (без автопілота)

**Мета:** переконатися, що плата читає GNSS і правильно логірує стан.

### 2.1. Підготовка

- Підключіть:
  - GNSS-приймач до плати (UART + 5 В + GND).
  - USB-порт плати до ПК.
  - 5 В живлення (від BEC / лабораторного блока).
- Встановіть швидкість порту в терміналі: **115200 бод** (або вашу `USB_BAUD`).

### 2.2. Кроки

1. Відкрийте серійний термінал (PlatformIO Serial Monitor, PuTTY, minicom тощо).
2. Подайте живлення на плату.
3. Дочекайтеся, поки GNSS отримає fix.

### 2.3. Що ви маєте побачити

- LED:
  - після старту — короткі блимання (ініціалізація);
  - далі — **повільне миготіння** (режим DR0, нормальна робота).
- У терміналі — рядки типу:

  ```text
  fix=3 s=18 hd=0.9 sp=0.0 crs=0.0 alt=176.3 DR0
  ```

Перевірте:

- `fix` = 3 (3D fix) або відповідний тип для вашого GNSS;
- `s` (satellites) — реалістичне число (10–20+ для сучасних модулів);
- `hd` (HDOP) — < 2.0 в хороших умовах;
- `DR0` — плата в нормальному режимі, без DR.

Якщо зʼявляється `DR1` або немає fix — дивіться `gnss_faq.md`.

---

## 3. Lab-тест №2 — імітація проблем GPS

**Мета:** перевірити, що **логіка DR та анти-спуфінг реально спрацьовує**.

> ⚠️ Не використовуйте нелегальні радіосигнали / спуфінг “по ефіру”.  
> Імітація має виконуватися або через симулятор/кабель, або у добре екранованому середовищі відповідно до законодавства.

### 3.1. Імітація втрати fix

Найпростіший варіант:

1. Дочекайтеся стабільного fix (`DR0`).
2. **Відʼєднайте антену GNSS** (або закрийте її екраном / віднесіть у приміщення).
3. Спостерігайте за USB-логами.

Очікувана поведінка:

- `fix` переходить у 0 або 1;
- `s` падає до малих значень;
- після спливу `GNSS_FIX_TIMEOUT_MS` та `DR_ENTER_MIN_BAD_MS` зʼявляється:

  ```text
  GPS: ENTER DR (no-fix)
  ... DR1
  ```

- LED переходить у “alarm”-режим (швидке миготіння / інший патерн, як описано у `gnss_led_status`).

### 3.2. Імітація грубого стрибка координат

Якщо у вас є:

- **GPS-симулятор по UART**, або
- можливість **програти записаний лог з іншого місця**,

ви можете:

1. Запустити плату з реальним GNSS-сигналом (наприклад, на вашій полігонній локації).
2. Раптово переключити GNSS-джерело на симульовані координати (інший регіон / півкуля).
3. Спостерігати реакцію.

Очікувана поведінка:

- у логах зʼявляється стрибок `lat/lon` на сотні/тисячі км;
- плата фіксує спуфінг (наприклад, `SPOOF: SOUTH HEMI JUMP` або “distance jump”);
- `GPS: ENTER DR (jump)` та `DR1`.

---

## 4. Інтеграційний тест — плата + ArduPilot

**Мета:** перевірити, що автопілот отримує GPS по MAVLink саме від цієї плати.

### 4.1. Підготовка

- Плата підключена до **GNSS** (як у Lab-тесті №1).
- `FC_SERIAL` зʼєднаний з відповідним **SERIALx** автопілота.
- В ArduPilot налаштовано (приклад для `SERIAL2`):
  - `SERIAL2_PROTOCOL = 2` (MAVLink2, або 1 якщо так у прошивці);
  - `SERIAL2_BAUD = 115`;
  - `GPS_TYPE` або `GPS_TYPE2 = 14` (MAV).

### 4.2. Кроки

1. Підключіться до автопілота через Mission Planner.
2. Відкрийте:
   - **CONFIG → Full Parameter Tree** — перевірте `SERIALx_*` і `GPS_TYPE`.
   - **DATA → Status** — подивіться поля `gpsstatus`, `gpshdop`, `gpsnsats`, `lat`, `lng`.
3. Дочекайтеся, поки плата вийде на DR0 (нормальний стан).

### 4.3. Що ви маєте побачити

- `gpsstatus` ≥ 3 (3D fix);
- `gpshdop` близький до `hd` у USB-логах;
- `gpsnsats` ≈ `s` у USB-логах;
- координати в Mission Planner збігаються з координатами з USB-логу.

За бажання:

- відкрийте **MAVLink Inspector** та знайдіть повідомлення:
  - `GPS_INPUT` (або `HIL_GPS`, залежно від конфігурації),
  - `STATUSTEXT` від `sysid = MY_SYSID` / `compid = MY_COMPID`.

---

## 5. Польовий тест — короткий політ

**Мета:** перевірити поведінку плати та автопілота в реальному польоті без навмисного спуфінгу.

### 5.1. Перед польотом

Переконайтеся, що:

- усі lab-тести пройдені;
- USB-лог на стоянці показує стабільний `DR0`;
- у Mission Planner немає повторюваних `GPS: ENTER DR` / інших помилок.

Збережіть параметри автопілота (`.param`) перед змінами.

### 5.2. Сценарій польоту

Рекомендовано:

1. Зліт у FBWA/CRUISE або AUTO з простою місією (1–2 кола).
2. Політ у зоні прямої видимості, без екстремальних маневрів.
3. Посадка.

Під час польоту:

- слідкуйте за поведінкою UAV (без ривків / провалів);
- за можливості дивіться вкладку **Messages** у Mission Planner (немає `ENTER DR`).

### 5.3. Аналіз після польоту

1. Збережіть `.bin`-лог автопілота.
2. За можливості — USB-лог плати до/після польоту.
3. Перевірте:

- чи не було неочікуваних входів у DR (`GPS: ENTER DR ...`);
- чи немає різких стрибків координат/швидкості в логах автопілота;
- чи узгоджені значення швидкості/висоти з очікуваними для вашого UAV.

---

## 6. Просунутий сценарій — тест з HackRF, gps-sdr-sim та PortaPack H2

> ⚠️ Дуже важливо: будь-яке **випромінювання GNSS-сигналів** має виконуватися **тільки в законний та безпечний спосіб**.  
> Використовуйте екрановані бокси, кабельні підключення та атенюатори, щоб сигнал **не виходив назовні**.  
> Мета цього сценарію — **лабораторний тест захисту від спуфінгу**, а не практичний спуфінг у полі.

### 6.1. Загальна ідея

- Ви створюєте **штучний GNSS-сценарій** (наприклад, “телепорт” в інший регіон або траєкторію).
- Цей сценарій програється:
  - або через **HackRF One**, підключений по кабелю до GNSS-приймача,
  - або через **PortaPack H2** (як автономний програвач IQ-файлу).
- Плата GNSS sanity board приймає та аналізує такий сигнал так само, як реальний ефір, і має:

  - **вчасно виявити аномалію**,  
  - **перейти в DR**,  
  - **залишити у логах зрозумілу причину**.

### 6.2. Підготовка безпечного стенду

1. **Фізичне підключення:**

   - Рекомендовано **кабельну схему**:
     - вихід HackRF/PortaPack → атенюатор(и) → GNSS-антенний вхід приймача;
     - обовʼязково використовуйте достатній атенюатор, щоб не перевантажити вхід.
   - Або використання **екранованого боксу**:
     - GNSS-антена, HackRF/PortaPack та плата — всередині металевого контейнера / RF-камери;
     - жодних отворів, через які сигнал може вийти.

2. **Живлення та звʼязок:**

   - HackRF One підключений до ПК по USB (коли працює з gps-sdr-sim).
   - PortaPack H2 може працювати автономно від акумулятора з попередньо завантаженим файлом.

3. **Безпека:**

   - перевірте локальне законодавство щодо використання SDR і генерації GNSS-сигналів;
   - працюйте на мінімальних рівнях потужності та лише в контрольованому середовищі.

### 6.3. Підготовка сценарію в gps-sdr-sim (концептуально)

На високому рівні:

1. Формуєте **NMEA-трек** або задаєте початкову позицію/траєкторію, яку хочете симулювати (наприклад, інший континент).
2. Використовуєте `gps-sdr-sim`, щоб згенерувати **IQ-файл** (наприклад, `scenario.bin`), сумісний з HackRF.
3. Варіанти сценарію:
   - “миттєвий телепорт” далеко від поточного місця;
   - плавний дрейф координат;
   - стрибки висоти / швидкості.

> Тут навмисно не наводиться покрокова інструкція або конкретні параметри генерації сигналу — вони залежать від вашої локальної конфігурації та мають використовуватися лише в законному лабораторному контексті.

### 6.4. Тест з HackRF One

1. **Запуск реальної сесії:**

   - Плата + GNSS працюють у нормальному режимі (`DR0`), записується USB-лог.
   - Автопілот можна поки не підключати (для спрощення).

2. **Перехід на симульований сигнал:**

   - Або механічно перемикаєте кабель GNSS з реальної антени на лінію HackRF;
   - або запускаєте сценарій у повністю екранованому стенді, де немає реального GNSS.

3. **Програвання IQ-файлу через HackRF:**

   - gps-sdr-sim передає заздалегідь згенерований сценарій;
   - GNSS-приймач починає “вірити” симуляції.

4. **Очікувана реакція плати:**

   - у певний момент координати/швидкості починають відрізнятися від фізично можливих;
   - плата фіксує:
     - або стрибок відстані (`GNSS_MAX_DIST_JUMP_M`),
     - або надмірну швидкість (`GNSS_MAX_SPEED_MPS` / `GNSS_MAX_SPEED_JUMP`),
     - або перехід у іншу півкулю (`GNSS_SOUTH_HEMI_PROTECT`).
   - у логах зʼявляється `GPS: ENTER DR (jump/spoof)` та відповідний `SPOOF: ...`;
   - LED переходить у “alarm”-режим, `DR1`.

5. **Аналіз:**

   - порівнюєте момент запуску сценарію з моментом, коли плата увійшла в DR;
   - за потреби коригуєте пороги в `config.h`.

### 6.5. Тест з PortaPack H2

PortaPack H2 можна використовувати як **автономний програвач** того самого IQ-файлу.

1. Перенесіть згенерований `scenario.bin` (або інший сумісний формат) на SD-карту PortaPack H2.
2. Запустіть програвання сценарію у **тому ж екранованому стенді / кабельній схемі**, що й для HackRF.
3. Повторіть кроки спостереження:

   - USB-лог плати;
   - стани DR0/DR1;
   - `STATUSTEXT` (якщо є звʼязок з автопілотом).

Плюс PortaPack: він дозволяє **повторювати один і той самий сценарій** без ПК, що зручно для регресійних тестів.

---

## 7. Тест “dry-run” (без впливу на GPS автопілота)

Якщо у прошивці реалізовано **режим dry-run**, плата:

- аналізує GNSS,
- генерує логи / `STATUSTEXT`,
- **але не підмінює** GPS для автопілота.

Такий режим зручний:

- для перших тестів на вже літючих платформах;
- для порівняння поведінки “з платою” і “без плати”.

Рекомендований сценарій:

1. Увімкніть dry-run в `config.h`.
2. Підключіть плату паралельно до того ж GNSS, який вже використовується автопілотом (Т-подібний спліттер по UART).
3. Літайте як зазвичай, а потім:

   - аналізуйте USB-логи плати,
   - дивіться, в яких моментах плата **пропонувала б** увійти в DR.

Якщо dry-run показує адекватну поведінку, можна переходити до повної інтеграції (GPS через плату).

---

## 8. Регресійні тести після зміни прошивки

Після кожної серйозної зміни (нові пороги, новий GNSS, нова логіка DR) рекомендується повторювати **мінімальний набір регресій**:

1. **Lab-тест №1:** базовий запуск з GNSS.
2. **Lab-тест №2:** імітація втрати fix.
3. **Інтеграційний тест з ArduPilot.**
4. **Короткий польовий тест.**
5. За потреби — повторні сценарії з HackRF / PortaPack (як у розділі 6).

> Важливо вести простий журнал:  
> *версія прошивки → дата тесту → які тести пройдені → примітки / виявлені нюанси.*

---

## 9. Що вважати успішним результатом

Можна вважати, що плата коректно інтегрована та протестована, якщо:

- ✅ Усі lab-тести проходять стабільно.
- ✅ В інтеграційних тестах ArduPilot бачить GPS без збоїв.
- ✅ У перших польотах **немає неочікуваних входів у DR** у нормальних умовах.
- ✅ При імітації проблем з GNSS (включно з HackRF / PortaPack у лабораторії) плата:
  - вчасно входить у DR,
  - зрозуміло логірує причину,
  - не віддає в автопілот завідомо фейкові координати.
- ✅ Усі зміни прошивки задокументовані у `gnss_changelog.md`, а характерні кейси — у `gnss_faq.md`.

---

Цей розділ можна доповнювати власними сценаріями тестів для конкретних платформ (катапультний старт, посадка на сітку, польоти на великі відстані тощо) — головне, щоб кожна нова версія прошивки була перевірена однаково чіткою процедурою.
